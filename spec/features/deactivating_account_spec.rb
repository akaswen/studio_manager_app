require 'rails_helper'

RSpec.feature "DeactivatingAccounts", type: :feature do
  xit("allows a user to deactivate their account from the edit profile page") do
  end

  xit("allows a user to reactivate their account when they sign in again") do
  end

  xit("deactivated users aren't shown in any of the teacher's views") do
  end

  xit("deactivation has friendly forwarding for teacher") do
  end
end
